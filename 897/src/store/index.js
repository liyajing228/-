import Vue from 'vue';
import Vuex from 'vuex';
Vue.use(Vue);

const state={}
const action={}
const mutations={}

export default new Vuex.Store({
    state,
    action,
    mutations,
    modules: {}
})
